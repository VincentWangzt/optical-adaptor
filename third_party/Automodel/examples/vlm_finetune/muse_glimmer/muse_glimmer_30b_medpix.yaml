# Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Convert the native MuseGlimmer checkpoint with the bundled hf/convert_muse_glimmer_to_hf.py,
# including --encoder_checkpoint_dir, before running this recipe.
#
# Run on one 8-GPU node:
#   automodel examples/vlm_finetune/muse_glimmer/muse_glimmer_30b_medpix.yaml --nproc-per-node 8

recipe: FinetuneRecipeForVLM

seed: 1234

step_scheduler:
  global_batch_size: 8
  local_batch_size: 1
  ckpt_every_steps: 1000
  val_every_steps: 20
  log_remote_every_steps: 1
  gc_every_steps: 5
  num_epochs: 1
  max_steps: 100

dist_env:
  backend: nccl
  timeout_minutes: 30

rng:
  _target_: nemo_automodel.components.training.rng.StatefulRNG
  seed: 1234
  ranked: true

model:
  _target_: nemo_automodel.NeMoAutoModelForCausalLM.from_pretrained
  pretrained_model_name_or_path: meta-models/Muse-Glimmer-30B
  torch_dtype: bfloat16
  attn_implementation: sdpa
  backend:
    attn: sdpa
  use_liger_kernel: false

processor:
  _target_: transformers.AutoProcessor.from_pretrained
  pretrained_model_name_or_path: meta-models/Muse-Glimmer-30B
  trust_remote_code: true

checkpoint:
  enabled: false
  checkpoint_dir: checkpoints/muse_glimmer_30b_medpix/
  model_save_format: safetensors
  save_consolidated: false

distributed:
  strategy: fsdp2
  dp_size: none
  tp_size: 1
  cp_size: 1
  pp_size: 1
  ep_size: 1
  sequence_parallel: false
  activation_checkpointing: true

freeze_config:
  freeze_vision_tower: true
  freeze_audio_tower: true
  freeze_language_model: false

loss_fn:
  _target_: nemo_automodel.components.loss.masked_ce.MaskedCrossEntropy

dataset:
  _target_: nemo_automodel.components.datasets.vlm.datasets.make_medpix_dataset
  path_or_dataset: mmoukouba/MedPix-VQA
  split: train

dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  # MuseGlimmer performs CUDA vision work in the model process. Forked dataloader
  # workers would inherit initialized CUDA/NCCL state and can hang at shutdown.
  num_workers: 0
  drop_last: true
  collate_fn:
    _target_: nemo_automodel.components.datasets.vlm.collate_fns.default_collate_fn
    max_length: 1024

validation_dataset:
  _target_: nemo_automodel.components.datasets.vlm.datasets.make_medpix_dataset
  path_or_dataset: mmoukouba/MedPix-VQA
  split: validation

validation_dataloader:
  _target_: torchdata.stateful_dataloader.StatefulDataLoader
  num_workers: 0
  drop_last: true
  collate_fn:
    _target_: nemo_automodel.components.datasets.vlm.collate_fns.default_collate_fn
    max_length: 1024

optimizer:
  _target_: torch.optim.AdamW
  betas: [0.9, 0.95]
  eps: 1e-8
  lr: 5.0e-6
  weight_decay: 0.1

lr_scheduler:
  lr_decay_style: constant

wandb:
  enable: false
  project: automodel-muse-glimmer
  entity: <your_wandb_entity>
  name: muse_glimmer_30b_medpix_100steps
  group: muse_glimmer_30b_loss_curves
  tags: [muse_glimmer, 30b, dense, vlm, medpix, fsdp2]
  dir: logs/muse_glimmer_30b_medpix/wandb

ci:
  recipe_owner: HuiyingLi
  nodes: 1
  time: "02:00:00"
  max_steps: 5
