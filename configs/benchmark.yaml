training_config: configs/training.yaml
output_dir: outputs/benchmarks/live-2500
checkpoint: outputs/adapter-v1/runs/mlp-10epochs/step-002500
seed: 42
multi_image_counts: [2, 4, 8]
long_source_lines: 80
qa_source_lines_per_image: 80
lcb_dataset: Steefano/LCB
lcb_revision: 989d5eff750d65a72c522e47f8f745ef2e22906b
prompt_token_limit: 32768
reconstruction_max_tokens: 20000
qa_max_tokens: 32
batch_size: 4
backend:
  max_model_len: 131072
  max_num_seqs: 4
  max_num_batched_tokens: 4096
  max_images: 128
  vision_batch_size: 4
  gpu_memory_utilization: 0.82
  enforce_eager: true
reconstruction_instruction: >-
  Transcribe all visible text in the images in the order given. Preserve line breaks,
  indentation, punctuation, and code formatting. Separate the transcriptions of adjacent
  images with one newline. Return only the transcription without commentary or Markdown fences.
